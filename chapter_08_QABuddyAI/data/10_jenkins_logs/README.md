# Source 10: Jenkins logs and results

Drop build console logs (`.log`, `.txt`) and JUnit result XMLs here.
Only failure blocks (stack trace + context) and a per-build summary are
indexed; passing-spam is skipped. Build number is read from the filename
(e.g. `build_128_console.log`). See `sample_build_128_console.log`.
