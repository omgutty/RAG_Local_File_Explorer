# Source 09: PRD / SRS / BRD / FRD

Drop requirement documents here as `.pdf` (or `.md`).
Chunked heading-aware with page numbers kept, so answers cite `PRD.pdf p.7`.
A VWO PRD fixture is copied here by `scripts/setup_fixtures.sh`.
